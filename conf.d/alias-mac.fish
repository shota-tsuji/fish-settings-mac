alias ls='ls -G'
alias l='ls -l -h -t -A -G -F'

# `coreutils` package is needed.
alias readlink='greadlink'
alias date='gdate'

# `gnu-sed` package is needed.
alias sed='gsed'

# `gawk` package is needed.
alias awk='gawk'
